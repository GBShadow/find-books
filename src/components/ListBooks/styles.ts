import styled from 'styled-components';

export const Button = styled.button`
  width: 150px;
  padding: 0.8rem;
  margin-top: 1.6rem;
  color: var(--blue-100);
  background-color: transparent;
  border: 0;
`;
